# AA Scooters handoff — resume here (2026-08-21)

Paste this whole message into a new Claude/Cowork session to pick up where
this one left off (previous session ran low on usage). Context: this is
the AA Scooters rental staff app, a Vercel/Node app backed by Google
Sheets/Drive JSON, living at
`/Users/anton/AA-Scooters-Project Database/vercel-site/` (repo pushed to
GitHub, deployed via `git push`, no more Hostgator).

**Read `/Users/anton/AA-Scooters-Project Database/CLAUDE.md` in full before
touching anything.** Hard-won rules that apply directly:
- Any `Code.gs` edit needs a manual redeploy (paste into Apps Script editor
  → Deploy → Manage deployments → Edit → New version) — the file on disk
  does nothing on its own.
- Files in this folder have historically silently reverted to old versions
  after a successful-looking write. After every edit, re-read the file back
  (fresh) and grep for something distinctive you just added before trusting
  the save.
- Never run `git` commands through a sandboxed/device bash tool that mounts
  this real folder — it self-inflicts a `.git/index.lock` that blocks
  Anton's own Terminal, which the sandbox can't clean up. Prepare exact
  `git add/commit/push` and have Anton run them himself.
- Standing workflow (per Anton): one task at a time. Deliver, test, hand
  over exact git commands, then STOP and wait for Anton to push and confirm
  before starting the next task. Do not chain tasks autonomously.
- Also check `PROGRESS.md` in `vercel-site/` — it's the project's own
  running handoff log, newest entries at the top.

## Just finished and pushed (confirmed by Anton)

Two related, already-live fixes, both in `vercel-site/`:

1. Commit `4f5b34b` — Revolut added as a deposit option (was missing from
   the security-deposit dropdown/edit-form allow-list).
2. Commit `b1a5ae7` — replaced best-effort name/amount ledger matching
   (flagged by Anton as too risky for real money) with a direct
   contract→ledger-row reference stored in the Contract sheet's previously-
   unused 37th column, plus a manual picker modal (`#depositPickBackdrop`
   in `contract.html`) for any contract with no stored reference. Backend
   in `lib/contractWrites.js`: `buildDepositRefB`/`parseDepositRefB`,
   `listOpenSecurityDepositsFromJson`, `clearSecurityDepositAtRowFromJson`,
   `clearSecurityDepositByRefFromJson`, `resolveDepositLedgerPickFromJson`,
   restructured `editContractFromJson`. Full writeup with the two
   self-caught bugs (breadcrumb-loss, double-log risk) is at the top of
   `PROGRESS.md`.

**Still outstanding on this feature — not yet done, worth doing soon:** a
live smoke test against the real spreadsheet (only tested against a mocked
sheetIO so far). Create a contract, take a Revolut deposit, edit it back to
Cash, confirm the Revolut ledger row actually clears and the contract's
hidden ref column ends up blank. Then try an OLD pre-existing contract
(one with nothing in that 37th column) and confirm the picker modal shows
up and works.

## Two separate pending threads — ask Anton which to start

Both are real, neither has been started. It wasn't clear from context which
one Anton means by "continue with this," so ask him directly.

**Thread A — full Code.gs vs vercel-site parity audit.** Anton explicitly
asked for this after the Revolut gap was found: "go through every function
of the code GS and this previous app and make sure everything is there."
Scope: go through every function in `Code.gs` (~748KB, old Apps Script
backend) and confirm each has a real, complete counterpart in the
Vercel/Node rewrite (`lib/*.js`); flag (a) functions missing entirely, (b)
stub/warning-only ports (the deposit-ledger-sync gap that was just fixed is
exactly this pattern), (c) counterparts that are logically different from
the original. Deliver as a markdown checklist, similar style to the
existing `FORMULA_CASCADE_AUDIT_2026-08-14.md` in this folder — use it as
the template for format/tone. Given the size of `Code.gs`, this will likely
need to be split into batches (e.g. by feature area — contracts, bikes,
customers, accounts, deposits, calendar sync — one pass per area) rather
than one single read-through.

**Thread B — an existing, never-started bug punch list.** While preparing
this handoff, found `BUGFIX_HANDOFF.md` sitting untracked (never
committed, never actioned per `git log`/`git status`) in `vercel-site/`,
dated 2026-08-18. It's a fully-written 7-item fix-it list from a prior full
QA pass (see `TESTING.md` in the same folder for full repro detail):
1. All bike photos 404 (suspected Vercel Hobby-plan 12-function serverless
   cap issue)
2. Cash-sheet-drift bug on the Accounts page (flagged HIGH business
   impact)
3. Stuck "Saving…" indicator on Rent/Extend/Swap Bike
4. Bike-name autocomplete double-space bug
5. Extend form "Amount paid" concatenation bug
6. ServiceWorker console exception on pricing.html
7. Possible/unconfirmed — wrong default date on a new Pending contract

`BUGFIX_HANDOFF.md` itself already has full repro/fix detail for each and
is written in the same "paste into a new session" format as this file — it
can likely be pasted in directly once Anton says to start on it.

## Immediate next step for the new session

Do NOT start either thread automatically. Ask Anton: "Continue with the
Code.gs audit, or the bug punch list in BUGFIX_HANDOFF.md first?" — then
follow the standing one-task-at-a-time workflow from there.
